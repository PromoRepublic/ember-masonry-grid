{{#each items as |item index|}}
  {{#if customLayout}}
    {{yield item index this}}
  {{else}}
    <MasonryItem
            @grid={{this}}
            @item={{item}}
            @onItemClick={{onItemClick}}>
      {{yield item index this}}
    </MasonryItem>
  {{/if}}
{{/each}}
